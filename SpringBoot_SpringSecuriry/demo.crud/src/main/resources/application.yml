server:
  port: 8088
  servlet:
    context-path: "/hello"
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/student"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true